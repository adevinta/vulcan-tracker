language: go
go:
  - 1.19.x
env:
  global:
  - CGO_ENABLED=0
  - GO111MODULE=on
gobuild_args: -a -tags netgo -ldflags '-w'
go_import_path: github.com/adevinta/vulcan-local-api
script:
  - go install ./...
  - go test -short -v $(go list ./... | grep -v /vendor/)
